
import auth from '../auth';

const run = async () => {
    // const verify = await auth.verify({ email: 'admin@puzzlehr.com' });
    // console.log(verify);

    // const results = await auth.signIn({ email: 'admin@puzzlehr.com', code: '136363' });
    // console.log(results);
};

export default { run };
